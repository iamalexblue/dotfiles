-- ==========================================================================
-- WakaTime - 自动编程时间追踪 (https://wakatime.com/vim)
-- 需要在启动时加载，不能用 lazy 延迟加载
-- ==========================================================================
return {
  "wakatime/vim-wakatime",
  lazy = false,
}
